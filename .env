# Client-side environment variables (safe to expose)
REACT_APP_API_ENDPOINT=/api/generate

# Note: Sensitive server-side variables (WEBHOOK_URL, WEBHOOK_AUTH_TOKEN)
# should be set in Vercel dashboard environment variables, not here